package isw.backend.repository;

import isw.backend.model.MiembroArea;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MiembroAreaRepository extends JpaRepository<MiembroArea, Long> {
}
